persist messages in the session

change v2 to just just be message

Thoughts:

Only run with stdin stdout when run as main

Should I build this using a server?

Let's build a functional cli first and then use that as the basis for a more generalized server
Should this run locally in react native?
