pre-commit:
  commands:
    fmt:
      run: mise exec -- bun run fmt
    lint:
      run: mise exec -- bun run lint:check

pre-push:
  commands:
    typecheck:
      run: mise exec -- bun run typecheck
